const mutations = {
    SET_MENUS(state, menus) {
        state.menus = menus;
    },
    SET_SEATS(state, seats) {
        state.seats = seats;
    },
    SET_ORDER(state, order) {
        state.order = order;
    },
};

export default mutations;
